# main: демо-бот MAX (без оркестратора). Только BOT_TOKEN.

BOT_TOKEN=

# Если картинка по URL без заголовка с токеном не качается — 1.
MAX_IMAGE_USE_BOT_AUTH=0

# Webhook (src/webhook.mjs)
WEBHOOK_SECRET=
PORT=8080
WEBHOOK_PATH=/webhook
