services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    command: ["node", "src/polling.mjs"]
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      MAX_IMAGE_USE_BOT_AUTH: ${MAX_IMAGE_USE_BOT_AUTH:-0}
